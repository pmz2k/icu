# Security (REQUIRED)
SECRET_SALT=your-secret-salt-here-generate-with-openssl-rand-hex-32

# Database (optional - defaults to SQLite)
DATABASE_URL=sqlite:///./epr.db

# Server (optional)
HOST=0.0.0.0
PORT=8000
